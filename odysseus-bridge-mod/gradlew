#!/usr/bin/env bash
# Placeholder gradlew — will be regenerated by gradle on first CI run.
# If CI complains, run `gradle wrapper` once locally OR let GitHub Actions
# use the pre-installed gradle to bootstrap.
exec gradle "$@"
